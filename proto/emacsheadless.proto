syntax = "proto3";

package headless;
option go_package = ".;proto";

service Headless {
  rpc Connect(stream Cmd) returns (stream Cmd) {};
}

enum CmdType {
  NewClient = 0;
  DrawChar = 1;
}

message Cmd {
  CmdType type = 1;
}

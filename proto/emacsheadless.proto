syntax = "proto3";

package headless;
option go_package = ".;headless";

service Headless {
        rpc SendMessage(stream Cmd) returns (stream Cmd) {};
}

enum CmdType {
     DrawChar = 0;
}

message Cmd {
        CmdType type = 1;
}
